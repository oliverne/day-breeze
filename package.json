{
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "test:unit": "lerna exec -- yarn test --coverage --ci",
    "dev:web": "lerna exec --scope @olv/day-breeze-web -- yarn dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "2.19.0",
    "lerna": "3.20.2"
  }
}
