{
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "test:unit": "lerna exec -- yarn test --coverage --ci",
    "dev": "lerna exec --scope @olv/day-breeze-web -- yarn dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "3.10.1",
    "lerna": "3.22.1"
  }
}
